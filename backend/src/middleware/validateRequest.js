/**
 * validateRequest.js
 * Request body and parameter validation middleware.
 * Ensures required fields (email, phone, dates, service IDs) are present and properly formatted before processing.
 */

module.exports = {};
